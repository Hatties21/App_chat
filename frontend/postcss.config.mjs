// PostCSS configuration for Tailwind CSS v3
// Using standard tailwindcss plugin (not @tailwindcss/postcss which is v4)
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
